{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Google Analytics Data API v1beta - DateRange Type",
  "description": "A contiguous set of days: startDate, startDate + 1, ..., endDate",
  "type": "object",
  "properties": {
    "startDate": {
      "type": "string",
      "description": "The inclusive start date for the query",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$|^today$|^yesterday$|^[0-9]+(daysAgo)$"
    },
    "endDate": {
      "type": "string",
      "description": "The inclusive end date for the query",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$|^today$|^yesterday$|^[0-9]+(daysAgo)$"
    },
    "name": {
      "type": "string",
      "description": "Assigns a name to this date range"
    }
  },
  "required": ["startDate", "endDate"],
  "metadata": {
    "apiType": "type",
    "description": "Defines a date range for querying Google Analytics data"
  }
}

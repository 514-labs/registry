{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Google Analytics Data API v1beta - Comparison Type",
  "description": "Defines a comparison segment for filtering dimension values in a report",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "A saved name for this comparison"
    },
    "dimensionFilter": {
      "$ref": "#/definitions/FilterExpression",
      "description": "A basic comparison filter"
    },
    "comparison": {
      "type": "string",
      "description": "A saved comparison identified by the comparison's resource name"
    }
  },
  "oneOf": [
    { "required": ["dimensionFilter"] },
    { "required": ["comparison"] }
  ],
  "definitions": {
    "FilterExpression": { "type": "object" }
  },
  "metadata": {
    "apiType": "type",
    "description": "Defines comparison segments for comparative analysis in reports"
  }
}

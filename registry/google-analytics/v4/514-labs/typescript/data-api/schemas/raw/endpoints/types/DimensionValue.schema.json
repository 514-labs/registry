{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Google Analytics Data API v1beta - DimensionValue Type",
  "description": "The value of a dimension",
  "type": "object",
  "properties": {
    "value": {
      "type": "string",
      "description": "Value as a string if the dimension type is a string"
    },
    "oneValue": {
      "type": "string",
      "description": "If a dimension value represents a single value"
    }
  },
  "oneOf": [{ "required": ["value"] }, { "required": ["oneValue"] }],
  "metadata": {
    "apiType": "type",
    "description": "Represents dimension values in report responses"
  }
}

{
  "name": "@connectors/ads-b",
  "version": "1.0.0",
  "description": "ADS-B.lol API connector for real-time aircraft tracking",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "node run-tests.js",
    "test:unit": "node simple-test.js", 
    "test:jest": "pnpm exec jest",
    "test:watch": "pnpm exec jest --watch",
    "test:coverage": "pnpm exec jest --coverage",
    "test:live": "pnpm run build && node test-live.js",
    "test:all": "pnpm run test:unit && pnpm run test:live",
    "test:quick": "pnpm run build && node -e \"console.log('Quick smoke test...'); const {AdsbConnector} = require('./dist/index.js'); const c = new AdsbConnector(); console.log('✅ Connector loads successfully');\""
  },
  "keywords": [
    "ads-b",
    "aircraft",
    "tracking",
    "aviation",
    "api",
    "connector"
  ],
  "dependencies": {
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-nocheck
/* eslint-env jest */ /* global jest, describe, it, expect, beforeEach */

/**
 * Integration test for observability features in the HubSpot to ClickHouse pipeline
 * 
 * This test demonstrates how the observability features work within the context
 * of this Moose project, showing how they can be used to monitor and debug
 * the data pipeline from HubSpot to ClickHouse.
 * 
 * NOTE: This test is currently disabled due to missing dependencies.
 */

describe("HubSpot Observability Integration", () => {
  it.skip("should integrate with external Moose pipeline (requires external dependencies)", () => {
    // This test is skipped because it requires external Moose pipeline context
    // that is not available in this isolated connector test environment
  });
});
# OpenWeather API credentials
OPENWEATHER_API_KEY=your_api_key_here

# Optional: Default units (standard, metric, imperial)
OPENWEATHER_UNITS=metric

# Optional: Default language code (e.g., en, es, fr)
OPENWEATHER_LANG=en


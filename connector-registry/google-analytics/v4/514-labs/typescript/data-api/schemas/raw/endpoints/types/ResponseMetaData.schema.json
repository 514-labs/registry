{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Google Analytics Data API v1beta - ResponseMetaData Type",
  "description": "Response metadata for the report",
  "type": "object",
  "properties": {
    "dataLossFromOtherRow": {
      "type": "boolean",
      "description": "If true, the report results may be affected by data loss from other rows"
    },
    "schemaRestrictionResponse": {
      "type": "object",
      "description": "Describes the schema restrictions actively enforced in creating this report",
      "properties": {
        "activeMetricRestrictions": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "restrictedMetricTypes": {
                "type": "array",
                "items": { "$ref": "#/definitions/MetricType" }
              },
              "metricName": { "type": "string" }
            }
          }
        }
      }
    },
    "currencyCode": {
      "type": "string",
      "description": "The currency code used in this report"
    },
    "timeZone": {
      "type": "string",
      "description": "The property's time zone"
    },
    "emptyReason": {
      "type": "string",
      "description": "If empty reason is specified, the report is empty for this reason"
    },
    "subjectToThresholding": {
      "type": "boolean",
      "description": "If subjectToThresholding is true, this report is subject to thresholding"
    }
  },
  "definitions": {
    "MetricType": { "type": "string" }
  },
  "metadata": {
    "apiType": "type",
    "description": "Contains metadata about report responses"
  }
}

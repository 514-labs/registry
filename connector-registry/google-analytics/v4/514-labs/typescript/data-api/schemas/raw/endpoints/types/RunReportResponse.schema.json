{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Google Analytics Data API v1beta - RunReportResponse Type",
  "description": "The response from a standard report request",
  "type": "object",
  "properties": {
    "dimensionHeaders": {
      "type": "array",
      "description": "Describes dimension columns",
      "items": { "$ref": "#/definitions/DimensionHeader" }
    },
    "metricHeaders": {
      "type": "array",
      "description": "Describes metric columns",
      "items": { "$ref": "#/definitions/MetricHeader" }
    },
    "rows": {
      "type": "array",
      "description": "Rows of dimension value combinations and metric values",
      "items": { "$ref": "#/definitions/Row" }
    },
    "totals": {
      "type": "array",
      "description": "If requested, the totaled values of metrics",
      "items": { "$ref": "#/definitions/Row" }
    },
    "maximums": {
      "type": "array",
      "description": "If requested, the maximum values of metrics",
      "items": { "$ref": "#/definitions/Row" }
    },
    "minimums": {
      "type": "array",
      "description": "If requested, the minimum values of metrics",
      "items": { "$ref": "#/definitions/Row" }
    },
    "rowCount": {
      "type": "integer",
      "description": "The total number of rows in the query result"
    },
    "metadata": {
      "$ref": "#/definitions/ResponseMetaData",
      "description": "Metadata for the report"
    },
    "propertyQuota": {
      "$ref": "#/definitions/PropertyQuota",
      "description": "This Analytics Property's quota state"
    },
    "kind": {
      "type": "string",
      "description": "Identifies what kind of resource this message is"
    }
  },
  "definitions": {
    "DimensionHeader": { "type": "object" },
    "MetricHeader": { "type": "object" },
    "Row": { "type": "object" },
    "ResponseMetaData": { "type": "object" },
    "PropertyQuota": { "type": "object" }
  },
  "metadata": {
    "apiType": "type",
    "description": "Response structure for standard report requests"
  }
}

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Google Analytics Data API v1beta - MetricHeader Type",
  "description": "Describes a metric column in the report response",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The metric's name"
    },
    "type": {
      "$ref": "#/definitions/MetricType",
      "description": "The metric's data type"
    }
  },
  "required": ["name"],
  "definitions": {
    "MetricType": { "type": "string" }
  },
  "metadata": {
    "apiType": "type",
    "description": "Describes metric columns in report responses"
  }
}
